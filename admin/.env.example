# Grahachara Marketing Admin - Environment Variables
# Copy this to .env.local and fill in your values

# Gemini API Key (same one from server/.env)
GEMINI_API_KEY=your_gemini_api_key_here

# Server URL (your astrology API)
# Production: https://api.grahachara.com — use http://localhost:3000 for a local server.
NEXT_PUBLIC_SERVER_URL=https://api.grahachara.com

# Pexels API Key (free at https://www.pexels.com/api/)
# Optional — if missing, reels use CSS-only element-themed backgrounds
PEXELS_API_KEY=your_pexels_api_key_here

# Optional: set both to 1 to skip Google auth in LOCAL DEV ONLY (never in prod builds)
DEV_NO_AUTH=
NEXT_PUBLIC_DEV_NO_AUTH=
